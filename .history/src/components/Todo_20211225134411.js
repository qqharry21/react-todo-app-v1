/** @format */

import React, { useState } from 'react';

function Todo({ todos }) {
  const [edit, setEdit] = useState({ id: null, value: '' });
  return;
  todos.map((todo, index) => {});
}

export default Todo;
